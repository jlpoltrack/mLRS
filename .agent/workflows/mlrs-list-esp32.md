---
description: List available ESP32 hardware targets
---
Lists all ESP32 targets (PlatformIO environments) from `platformio.ini`.

// turbo
1. List all ESP32 environments:
   `python tools/run_make_esp_firmwares.py --list-targets --nopause`

> [!NOTE]
> ESP32 target names are like `rx-generic-2400-d-pa` or `tx-generic-2400-v2`.
